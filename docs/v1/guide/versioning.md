# Vue 2 maintenance policy

<div class="version-strip">VUE 2 TRACK · package v1.x · maintained</div>

The `1.x` package line accepts Vue `^2.6.14` and `^2.7.0`. It receives compatibility, packaging, accessibility and documented bug-fix updates without changing its Vue peer dependency.

For Vue 3 applications, use the [Vue 3 / package v2 documentation](/).
