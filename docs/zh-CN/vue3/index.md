# Vue 3 迁移指南

Vue 3 实现是当前的发布版本线，并以默认的 `latest` 版本安装。

## 契约

- 原生 Vue 3 组件与插件注册。
- Vue `^3.5.0` 同级依赖。
- 在可行时保持等效的核心 props、事件与视觉行为。
- 为每个重命名或移除的 API 提供迁移对照表。
- 专门的单元、SSR 与真实浏览器覆盖。

当前的 `1.x` 软件包仍是 Vue 2 版本线。`2.x` 保留了公共组件名、props、事件、插槽与导航方法；Vue 2 用户应继续使用 `vue-carousel-3d@1`。
